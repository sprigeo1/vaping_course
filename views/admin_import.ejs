<%- include('layout', { body: (function(){/*
<section class="card">
  <h2>Bulk Import Learners (CSV)</h2>
  <p>CSV headers: <code>first_name,last_name,code,school_ids,course_ids</code>. Multiple values use semicolons (e.g., <code>1;3;5</code>).</p>
  <form method="post" action="/admin/import" enctype="multipart/form-data">
    <div class="row"><label>CSV File</label><input type="file" name="csv" accept=".csv" required></div>
    <button class="button" type="submit">Import</button>
  </form>
</section>
*/}).toString().slice(14,-3) }) %>
